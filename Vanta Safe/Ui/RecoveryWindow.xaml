<Window x:Class="Vanta_Safe.Ui.RecoveryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Vanta_Safe.Ui"
        mc:Ignorable="d"
        Title="RecoveryWindow" Height="450" Width="800">

    <Window.Background>
        <ImageBrush ImageSource="G:\My CS\Semester 4\ISL\IS Final Project\Vanta Safe\Assets\bg3.jpeg" Stretch="UniformToFill"/>
    </Window.Background>
    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="350" Margin="10,0,0,0" Background="Black">

            <TextBlock Text="Account Recovery" 
                       Foreground="#00FF00" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"
                       Margin="0,20,0,20"/>

            <TextBox Style="{StaticResource HintTextbox}" 
                     Tag="Enter Username" 
                     x:Name="UsernameTextBox" 
                     Margin="10"
                     Height="30"/>

            <TextBox Style="{StaticResource HintTextbox}" 
                     Tag="Enter Secret Key" 
                     x:Name="SecretKeyTextBox" 
                     Margin="10"
                     Height="30" TextChanged="SecretKeyTextBox_TextChanged"/>

            <Button Style="{StaticResource ActionButtonStyle}" 
                    Content="Recover Account" 
                    Click="RecoverButton_Click" />

            <Button Style="{StaticResource DangerButtonStyle}" 
                    Content="Cancel" 
                    Click="CancelButton_Click" />

            <TextBlock x:Name="StatusMessage"
                       Foreground="Red"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
